Questões da aula 2

1) O nome "Linux" deriva da frase "Linus' Unix", onde Linus era o desenvolvedor do Linux e Unix é o modelo de sistema operacional no qual
     o Linux se baseia.

2) Daemons são programas que executam processos em background.

3) Shell, ou linha de comando é é uma ferramenta de interpretação de comandos.

4) Permanecer constantemente em modo de super-usuário deixa o sistema vulnerável.

5) Tabulação(TAB)

6) As setas, ctrl+r e outros.

7) 
     a) help [comando}
     b) ls -la
     c) pwd
     d) cd [pasta]
     e) mkdir [pasta]
     f) rm [arquivo]
     g) rmdir [pasta]
     h) cp [arquivo]
     i) cp
     j) mv [arquivo][destino]
     k) mv 
     l) rm(dir)
     m) cat [arquivo]
     n) file [arquivo]
     o) clear
     p) grep [palavra]
     q) sort
     r)
     s) cmp [arquivo1] [arquivo2]
     t) echo [texto]
